import { Fragment } from "react";
import Navigation from "../navigation/navigation";

const Layout = () => {
  return (
    <Fragment>
      <Navigation />
    </Fragment>
  );
};

export default Layout;
