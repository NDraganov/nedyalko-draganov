import Navigation from "../navigation/navigation";

const { Fragment } = require("react");

const Layout = () => {
  return (
    <Fragment>
      <Navigation />
    </Fragment>
  );
};

export default Layout;
